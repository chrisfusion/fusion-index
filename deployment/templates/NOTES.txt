fusion-index has been deployed.

  Release:   {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}

Services:
  Backend API:  http://{{ include "fusion-index.backendServiceName" . }}.{{ .Release.Namespace }}.svc.cluster.local:8080

{{- if .Values.ingress.enabled }}

Ingress:
  http{{ if .Values.ingress.tls.enabled }}s{{ end }}://{{ .Values.ingress.host }}

  If using minikube, add the following to /etc/hosts:
    $(minikube ip)  {{ .Values.ingress.host }}

API smoke-test:
  curl -s http{{ if .Values.ingress.tls.enabled }}s{{ end }}://{{ .Values.ingress.host }}/api/v1/artifacts | python3 -m json.tool
{{- end }}

{{- if .Values.postgresql.enabled }}

PostgreSQL:
  Host: {{ include "fusion-index.dbHost" . }}
  DB:   {{ include "fusion-index.dbName" . }}
  User: {{ include "fusion-index.dbUsername" . }}
{{- else }}

PostgreSQL (external):
  Host: {{ .Values.postgresql.external.host }}:{{ .Values.postgresql.external.port }}
{{- end }}

{{- if ne .Values.backend.storageBackend "FILESYSTEM" }}

S3 artifact storage:
  Bucket:   {{ .Values.s3.bucket }}
  Region:   {{ .Values.s3.region }}
{{- if .Values.s3.endpointOverride }}
  Endpoint: {{ .Values.s3.endpointOverride }}
{{- end }}
{{- else }}

Artifact storage: FILESYSTEM ({{ .Values.backend.storageFSRoot | default "~/.fusion-index/artifacts" }})
{{- end }}
